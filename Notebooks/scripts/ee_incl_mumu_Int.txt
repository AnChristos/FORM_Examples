       + pow(s,-1)*alpha*pow(gweak,2) * ( 4/( - pow(mZ,2) + s)*pow(ca,2)*pow(
         u,2)*pi - 4/( - pow(mZ,2) + s)*pow(ca,2)*pow(t,2)*pi + 4/( - pow(
         mZ,2) + s)*pow(cv,2)*pow(u,2)*pi + 4/( - pow(mZ,2) + s)*pow(cv,2)*
         pow(t,2)*pi );
;
